{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for Slate's menu-bar popover and full-app window",
  "windows": ["main", "popover"],
  "permissions": ["core:default"]
}
