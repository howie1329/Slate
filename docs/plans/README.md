# Implementation Plans

Generated by the improve skill on 2026-07-23. Execute in the order below unless dependencies say otherwise. Each executor must read its plan completely, honor its STOP conditions, and update its row when finished.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| 015 | Make sidecar bootstrap reproducible | P1 | S | — | TODO |
| 016 | Bound task context before sending it to the AI sidecar | P1 | S | — | TODO |
| 017 | Align agent guidance with the implemented AI workflow | P2 | S | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale).

## Dependency notes

- The three plans are independent. Execute 015 and 016 first because they affect the new feature's developer and runtime reliability; 017 is a documentation-only correction.
- Do not combine the plans in one change: 015 changes onboarding/diagnostics, 016 changes the native data boundary, and 017 changes repository guidance.

## Findings considered and rejected

- Root `npm audit` reports three moderate transitive advisories through the pre-existing `shadcn` CLI dependency. They predate this branch, are not on the new runtime path, and the available automated upgrade is breaking; do not fold that migration into these branch-fix plans.
- The production web bundle emits a chunk-size warning. It was observed during validation, but this branch-only audit did not establish that the regression was introduced here; do not optimize it without a baseline comparison.
